## Modules - a set of extra components 
---------

* ### **cvDnnDetect**
  *cvDnnDetect* use opencv's dnn detectors, which in turn load models of caffe, darknet, tensorflow, torch.  
  Dependency: opencv > 3.4;
  [running source](moduleTest/testCvDnnDetect.cpp)
  
